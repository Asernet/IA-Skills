# Automation Scripts for Video Creation

In questa cartella è possibile inserire script (Python, Bash, etc.) per automatizzare parti del processo.

Esempi di futuri script utili:
1. `gpt_to_voiceover.py`: Per inviare lo script a una TTS API (es. ElevenLabs).
2. `metadata_generator.py`: Per generare automaticamente file .json pronti per il caricamento su YouTube.
3. `prompt_expander.py`: Per trasformare descrizioni brevi in prompt densi per generatori specifici.

---
Vedi il manuale in `SKILL.md` per i dettagli su come integrare nuovi strumenti.
